# For 循环示例

# 定义用户数组
@users json`[
  {"id": 1, "name": "Alice"},
  {"id": 2, "name": "Bob"},
  {"id": 3, "name": "Charlie"}
]`

# 基本循环
for $user in $users
  post "https://httpbin.org/post"
  headers
    Content-Type "application/json"
  body
    user_id $user.id
    user_name $user.name

---

# 带索引的循环
for $index, $user in $users
  post "https://httpbin.org/post"
  body
    position $index
    user_id $user.id
    user_name $user.name

